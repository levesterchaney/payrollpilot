rootProject.name = 'payrollpilot'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("gradle/libs.versions.toml"))
        }
    }
}

include ':backend'
include ':frontend'
include ':shared'